<Page x:Class="Shoes.Pages.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Shoes.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="MainPage">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200*"/>
            <ColumnDefinition Width="200*"/>
            <ColumnDefinition Width="200*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Column="2" Grid.Row="0" HorizontalAlignment="Right" Margin="10">
            <TextBlock Name="tblUser"/>
        </StackPanel>

        <TextBox Grid.Column="0" Grid.Row="1" Name="tbSearch" SelectionChanged="tbSearch_SelectionChanged" Width="150" Height="30" Visibility="Collapsed"/>
        <ComboBox Grid.Column="1" Grid.Row="1" Name="cbFilter" SelectionChanged="cbFilter_SelectionChanged" Width="150" Height="30" Visibility="Collapsed"/>
        <ComboBox Grid.Column="2" Grid.Row="1" Name="cbSort" SelectionChanged="cbSort_SelectionChanged" Width="150" Height="30" Visibility="Collapsed"/>

        <ListView Name="LviewProducts" Grid.Column="0" Grid.ColumnSpan="3" Grid.Row="2" MouseDoubleClick="LviewProducts_MouseDoubleClick" d:ItemsSource="{d:SampleData ItemCount=2}" Margin="10">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="Black" BorderThickness="1" Background="{Binding Background}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="220"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                            </Grid.ColumnDefinitions>

                            <Image Width="200" Height="100" Margin="5" Source="{Binding ImgPath}"/>

                            <StackPanel Grid.Column="1" Margin="5">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding products_categories.title}" FontWeight="Bold"/>
                                    <TextBlock Text="|" FontWeight="Bold"/>
                                    <TextBlock Text="{Binding title}" FontWeight="Bold"/>
                                </StackPanel>
                                <TextBlock Text="{Binding description, StringFormat='Описание товара: \{0}'}" TextWrapping="Wrap"/>
                                <TextBlock Text="{Binding manufacturer, StringFormat='Производитель: \{0}'}"/>
                                <TextBlock Text="{Binding supplier, StringFormat='Поставщик: \{0}'}"/>
                                <TextBlock Text="{Binding price, StringFormat='Цена: \{0} руб'}"/>
                                <TextBlock Text="{Binding units.title, StringFormat='Еденица измерения: \{0}'}"/>
                                <TextBlock Text="{Binding quantity_in_stock, StringFormat='Количество на складе: \{0}'}"/>
                            </StackPanel>

                            <TextBlock Grid.Column="2" Text="{Binding discount, StringFormat='\{0}%'}" VerticalAlignment="Center"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
