<Page x:Class="Shoes.Pages.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Shoes.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="MainPage" IsVisibleChanged="Page_IsVisibleChanged">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200*"/>
            <ColumnDefinition Width="200*"/>
            <ColumnDefinition Width="200*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Column="0" Grid.ColumnSpan="3" Grid.Row="0" Margin="10">
            <Button x:Name="btnToOrders" Content="Заказы" Click="btnToOrders_Click" Visibility="Collapsed" HorizontalAlignment="Left" Margin="15"/>
            <TextBlock Name="tblUser" HorizontalAlignment="Right"/>
        </StackPanel>

        <TextBox Grid.Column="0" Grid.Row="1" Name="tbSearch" SelectionChanged="tbSearch_SelectionChanged" Width="150" Height="30" Visibility="Collapsed"/>
        <ComboBox Grid.Column="1" Grid.Row="1" Name="cbFilter" SelectedValue="0" SelectedValuePath="id" DisplayMemberPath="title" SelectionChanged="cbFilter_SelectionChanged" Width="150" Height="30" Visibility="Collapsed"/>
        <ComboBox Grid.Column="2" Grid.Row="1" Name="cbSort" SelectedIndex="0" SelectionChanged="cbSort_SelectionChanged" Width="150" Height="30" Visibility="Collapsed"/>

        <ListView Name="LviewProducts" Grid.Column="0" Grid.ColumnSpan="3" Grid.Row="2" MouseDoubleClick="LviewProducts_MouseDoubleClick" d:ItemsSource="{d:SampleData ItemCount=2}" Margin="10">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="Black" BorderThickness="1" Background="{Binding Background}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="220"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                            </Grid.ColumnDefinitions>

                            <Image Width="200" Height="100" Margin="5" Source="{Binding ImgPath}"/>

                            <StackPanel Grid.Column="1" Margin="5">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding products_categories.title}" FontWeight="Bold"/>
                                    <TextBlock Text="|" FontWeight="Bold"/>
                                    <TextBlock Text="{Binding title}" FontWeight="Bold"/>
                                </StackPanel>
                                <TextBlock Text="{Binding description, StringFormat='Описание товара: \{0}'}" TextWrapping="Wrap"/>
                                <TextBlock Text="{Binding manufacturers.title, StringFormat='Производитель: \{0}'}"/>
                                <TextBlock Text="{Binding suppliers.title, StringFormat='Поставщик: \{0}'}"/>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding CostLineThrough}" Foreground="Red"/>
                                    <TextBlock Text="{Binding CostWithDiscount}"/>
                                </StackPanel>
                                <TextBlock Text="{Binding units.title, StringFormat='Еденица измерения: \{0}'}"/>
                                <TextBlock Text="{Binding quantity_in_stock, StringFormat='Количество на складе: \{0}'}"/>
                            </StackPanel>

                            <TextBlock Grid.Column="2" Text="{Binding discount, StringFormat='\{0}%'}" VerticalAlignment="Center"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <Button Grid.Column="1" Grid.Row="3" x:Name="btnAddProduct" Content="Добавить товар" Click="btnAddProduct_Click" Visibility="Collapsed" Width="200" Margin="15"/>
    </Grid>
</Page>
